---
import dayjs from 'dayjs';

const { date, format = 'dddd, MMMM D, YYYY' } = Astro.props;
const post_date = new Date(date);
---
{!!date ? (
	<time datetime={post_date.toISOString()} class={Astro.props.class}>
		{dayjs(date, 'YYYY-MM-DD').format(format)}
	</time>
) : null}
